# ---------------------------------------------------------------------------
# Automated Insurance Claim Approval Engine - Python dependencies
# ---------------------------------------------------------------------------
# Pin to compatible ranges. The engine degrades gracefully to "mock mode"
# when optional cloud SDKs are not installed, so local development and the
# test-suite work without any external credentials.
# ---------------------------------------------------------------------------

# Core data modelling & settings ------------------------------------------------
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0

# LLM orchestration & RAG -------------------------------------------------------
langchain>=0.2,<0.3
langchain-anthropic>=0.1,<0.2
langchain-community>=0.2,<0.3

# Anthropic Claude SDK ----------------------------------------------------------
anthropic>=0.25,<1.0

# Vector database ---------------------------------------------------------------
pinecone-client>=3.0,<4.0

# Data warehouse ----------------------------------------------------------------
snowflake-connector-python>=3.6,<4.0

# Observability -----------------------------------------------------------------
structlog>=24.1,<25.0
prometheus-client>=0.20,<1.0

# Utilities ---------------------------------------------------------------------
tenacity>=8.2,<9.0
numpy>=1.24,<2.0

# Dev / test (install via: pip install -r requirements-dev.txt) -----------------
